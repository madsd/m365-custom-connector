# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: m365-pubmed-connector
metadata:
  template: m365-pubmed-connector@1.0.0
services:
  mcp:
    project: .
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
